# AI Handoff — claude-code-hub-site-config

> 每次会话结束更新此文件。新会话开工第一件事：读本文件 + 三件套（`git branch --show-current` / `git status --short` / `git log --oneline -5`）。

## 项目约定（防 worktree 分叉）

1. **单主目录**：本仓库只在 `/root/claude-code-hub-site-config` 干活。不要 `git worktree add`，不要另开 clone 做同一项目。
2. **收工前必 commit**：验证过的改动不留在工作区过夜，用 `wip:` 前缀即可，message 写清改了什么/为什么/下一步。**这是唯一能让另一个 AI 从干净 checkout 也不丢改动的机制。**
3. **构建/部署只从主目录做**：`docker compose -f docker-compose.production.yaml build app`，绝不基于干净 checkout 构建。
4. **必须开第二个 worktree 时**：先确认主目录 `git status` 干净 → commit → 再操作；新 worktree 建好后第一件事 merge/pull 主分支。
5. **不动他人改动**：dirty worktree 不 reset/stash/clean/全局格式化。

## 当前基线

- 分支：`feat/site-based-provider-config`
- 最近提交：`2b294ef3`（wip 全量固化，2026-08-09）
- worktree 状态：**干净**（上次固化后无新增改动）

## 已固化的改动（均在 2b294ef3）

| 主题 | 涉及文件 | 状态 |
|---|---|---|
| 会话复用恢复 | `src/app/v1/_lib/proxy/provider-selector.ts`（findReusable + ensure，与 54dfef27^ 一致） | 已固化，待用户测试 |
| token usage 修复 | `src/lib/provider-testing/`（readBodyWithFirstTokenMs 等 completed 后等终止符再 cancel） | 已固化，live 验证过（12:30 轮全有 token） |
| 删除分组自动解绑 | `src/repository/provider-groups.ts` + `src/actions/provider-groups.ts` + 5 语言 i18n | 已固化，**未部署** |
| deepseek 价格 manual 保护 | `model_prices` 数据 + 价格同步跳过 manual 源 | 部分固化，数据改动在 DB 未落库到 git |
| provider-site / rate-limit / 充值倍率 / i18n | 各类 src + messages | 已固化 |

## 部署信息（重要）

- 目标：`http://192.168.1.23:23000`，live 容器 `claude-code-hub-app-1`（23000→3000）
- 部署命令：
  ```bash
  cd /root/claude-code-hub-site-config && docker compose -f docker-compose.production.yaml build app && docker compose -f docker-compose.production.yaml up -d --no-deps --force-recreate app
  ```
- 只重建 app；PostgreSQL/Redis **不重启**；构建前保留回滚 tag。
- 健康检查：`http://192.168.1.23:23000/api/actions/health` → 200
- **不要启动 preview compose**（23001），会与 live 抢同一个 PostgreSQL/Redis。
- provider key / token 只放内存变量，不打印、不进 argv/日志/工具输出。

## 部署后要测的清单

- [ ] 删 `gemini` 组（id 469）：provider 1584/1585（group_tag=gemini）应自动归入 default（groupTag→null）
- [ ] 删 `国模` 组（id 467）：provider 1573/1574 同理
- [ ] 删 `Kimi` 组（id 450，0 引用）：应正常
- [ ] deepseek-v4-flash-0731 价格：下一轮 30 分钟云同步后不被覆盖（DB id 60273 已切 manual）
- [ ] `default` 组仍不可删

## 已知注意

- `54dfef27` 中 forwarder.ts 的 streaming hedge 禁用是**有意为之，不要恢复**。
- live 镜像 `7254961f739d`（含 token usage 修复）；回滚 tag：`claude-code-hub-local:site-based-provider-config-rollback-20260809_115212`。
- 12:00:22 崩溃（uncaughtException write ECONNRESET）是否修未定论，观察即可。
- 定时测试 UI 间隔与调度器 poll 对齐（180s vs 30min）疑点未深查。
